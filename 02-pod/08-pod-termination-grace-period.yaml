apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  restartPolicy: Never
  terminationGracePeriodSeconds: 5
  containers:
    - name: my-ubuntu-container
      image: ubuntu
      args:
        - "sleep"
        - "3600"